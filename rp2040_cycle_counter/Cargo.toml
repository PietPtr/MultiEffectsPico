[package]
name = "rp2040_cycle_counter"
version = "0.1.0"
edition = "2024"
description = "Enables benchmarking the execution time in cycles on a RP2040 chip"

# TODO: move to its own repo

[dependencies]
cortex-m = "0.7.7"
rp2040-hal = "0.11.0"
